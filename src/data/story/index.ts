import type { StoryEventDef } from '../../core/types';
import { CHAPTER1_EVENTS } from './chapter1';

// 章ごとのストーリーイベントをまとめる。第2章以降はファイルを足してここに並べる
export const STORY_EVENTS: StoryEventDef[] = [...CHAPTER1_EVENTS];
